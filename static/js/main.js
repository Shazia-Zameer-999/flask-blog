document.addEventListener("DOMContentLoaded",()=>{const toggle=document.querySelector(".nav-toggle"),nav=document.querySelector(".site-nav");toggle?.addEventListener("click",()=>{const open=nav.classList.toggle("open");toggle.setAttribute("aria-expanded",String(open));toggle.querySelector("i").className=`bi bi-${open?"x":"list"}`});document.querySelectorAll(".toast button").forEach(button=>button.addEventListener("click",()=>button.parentElement.remove()));setTimeout(()=>document.querySelectorAll(".toast").forEach(toast=>toast.remove()),6000);document.querySelectorAll("form[data-confirm]").forEach(form=>form.addEventListener("submit",event=>{if(!confirm(form.dataset.confirm))event.preventDefault()}));document.querySelectorAll("[data-copy]").forEach(button=>button.addEventListener("click",async()=>{await navigator.clipboard.writeText(button.dataset.copy);button.innerHTML='<i class="bi bi-check2"></i>';button.setAttribute("aria-label","Link copied")}));document.querySelectorAll("[data-count-for]").forEach(output=>{const input=document.getElementById(output.dataset.countFor),update=()=>output.textContent=`${input.value.length} characters`;input.addEventListener("input",update);update()})});
